The most basic Eve program
```
search
    [#a_number value]
bind @browser
    [#div text: "hello world {{value}}"]
```


Set up the initial values (See, the order of the blocks of code does not matter as
now this is after the code to display the value)

```
commit
    [#a_number value: 2]
```
